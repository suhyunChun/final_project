import React,{useState} from "react";

function TextTypeGraph(props) {
    const [text, setText] = useState([...props.textData])
    return(
        <React.Fragment>

        </React.Fragment>
    );
}
export default TextTypeGraph